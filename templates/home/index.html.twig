{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
            <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
                <div class="bg-gradient bg-primary text-white text-center py-4">
                    <h2 class="fw-bold mb-1">🖼️ CrayonPicAuthBundle Demo</h2>
                    <p class="mb-0">Fun and experimental image-based authentication</p>
                </div>

                <div class="card-body p-5 text-center">
                    <p class="lead mb-4">
                        Instead of using a password, this demo lets you <strong>log in with an encoded image</strong>.
                        Keep your stamped image safe – you’ll need it every time you authenticate.
                    </p>

                    {% if user.username is defined %}
                        <h4 class="mb-3">👋 Welcome back, <span class="text-primary">{{ user.username }}</span>!</h4>
                        <p class="text-muted">You are successfully authenticated with your stamped image.</p>
                        <a href="{{ path('_logout_main') }}" class="btn btn-outline-danger btn-lg mt-3 px-4">
                            <i class="bi bi-box-arrow-right"></i> Logout
                        </a>
                    {% else %}
                        <p class="text-muted mb-4">
                            To try it out, first <strong>register with an image</strong>.
                            You’ll receive a stamped image that you must use to log in.
                        </p>
                        <div class="d-flex justify-content-center gap-3 flex-wrap">
                            <a href="{{ path('app_login') }}" class="btn btn-outline-primary btn-lg px-4">
                                <i class="bi bi-box-arrow-in-right"></i> Login
                            </a>
                            <a href="{{ path('app_register') }}" class="btn btn-outline-success btn-lg px-4">
                                <i class="bi bi-person-plus"></i> Register
                            </a>
                        </div>
                    {% endif %}
                </div>

                <div class="card-footer bg-light text-muted text-center small py-3">
                    ⚠️ This bundle is intended for <strong>fun</strong> and <strong>experimental</strong> purposes – not recommended for production or security-critical projects.
                    Learn more on <a href="https://github.com/ycatan-w/pic-auth-bundle" target="_blank">GitHub</a>.
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
